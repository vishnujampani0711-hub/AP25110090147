the program takes names, attendance, and marks for 5 students using arrays.

attendance is stored in a 5x5 matrix where each value is 1 or 0.

marks are also stored in a 5x5 matrix for 5 subjects.

the menu runs in a loop and the user can pick different options.

when attendance report is selected, the program counts how many classes each student attended and missed.

it then compares attendance totals and finds the student with highest and lowest attendance.

performance report adds marks of all subjects for every student and finds averages.

the program also identifies topper and the lowest scoring student.

the grade report calculates grade by checking the student’s average marks.

the program keeps running until the user chooses exit.
